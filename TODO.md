##### v0.19.0 
- [x] Card "Movement Summary" per piano resa collassabile (richiesta esplicita): di default mostra ora una vista snella (Direct Displacement, Trend Rate, Total Path Distance, Weekly Rate, Activity Gauge, Overall Movement Direction, ETA in forma compatta, grafico piccolo H-vs-V), un click sull'intestazione espande in-place alla vista completa già esistente (Monitoring Period, Position Change grezzo/normalizzato, Movement Interpretation, Movement Pattern, ETA per soglia con date/range/bootstrap). Stato espanso/collassato indipendente per piano (non condiviso, a differenza del trigger di ridimensionamento delle figure H-vs-V)
  - Estratto `MeterSummaryCard.js` (`src/components/common/`) dal corpo di `CrackMovementVisualizer.js` — la card era ~300 righe di JSX inline dentro una `.map()`, incompatibile con `useState` per-piano senza violare le Rules of Hooks
  - ETA in forma compatta nella vista snella: `3.4/8.8/– yr (1mm/2mm/5mm)`, con eventuale `(100%/100%/23% conf.)` quando il metodo selezionato produce un intervallo bootstrap (oggi solo Weighted Theil-Sen) — stessa soglia "già raggiunto" (✓) usata nella vista completa
  - Piccolo grafico H-vs-V nella vista snella: dimensione fissa (135px, la stessa della dimensione "shrunk" di `ResizableFigure`), non condivide il trigger di ridimensionamento condiviso — è un'anteprima, non un controllo
  - **Layout su richiesta esplicita**: vista snella riorganizzata a griglia 2 colonne — Direct Displacement/Trend Rate e Total Path Distance/Weekly Rate affiancati; Activity Gauge su tutta la larghezza (`md:col-span-2`); grafico H-vs-V e coppia Overall Movement Direction/ETA affiancati nell'ultima riga, una cella ciascuno
- [x] "(Normalized) Movement Interpretation": la riga "Based on normalized data" era in grassetto e precedeva i dati (si leggeva come un'affermazione, non come una nota) — spostata in fondo, in corsivo, come le altre note della pagina (es. "* Coordinates inverted to standard interpretation"), ed estesa per spiegare *perché* serve la normalizzazione invece di limitarsi a ripetere la parola
  - **Domanda esplicita durante la richiesta**: la distinzione "(Normalized)" è ancora significativa, o è un residuo di un confronto con un'interpretazione "raw" mai esistita? Verificato: non è mai esistita una sezione "Raw Movement Interpretation" (né nella cronologia git, né altrove nell'app) — ma la distinzione resta sostanziale, non residua. Da METHOD.md: senza normalizzazione, la stessa lettura raw (+0.5, -0.3) significa cose diverse per piani diversi (crack closing & wall rising per Pianterreno e Piano 2, crack expanding & wall sinking per Piano 1), perché i crack meter non sono tutti montati con lo stesso orientamento fisico. Il qualificatore "(Normalized)" nell'intestazione resta quindi corretto e utile — soprattutto perché "Position Change" (raw) è mostrato due celle sopra nella stessa card — e non è stato rimosso; solo la nota di supporto è stata spostata e resa più esplicita sul perché
- [x] Calcolo ETA: sostituita la vecchia secante a due punti (solo prima e ultima lettura) con una regressione robusta e pesata nel tempo:
  - `TheilSen.js`, `ETASolver.js`, `BootstrapEstimator.js`, `RegressionAnalyzer.js` (`src/services/calculations/`) + `useETAPredictions.js` (`src/hooks/`) + `regressionConfig.js` (`src/constants/`)
  - Regressione separata per x(t) e y(t) per piano, t = giorni reali dalla prima lettura; stimatore Theil-Sen pesato (nessuna lettura scartata), peso decrescente in base alla deviazione di ortogonalità di `AngleAnalyzer`
  - ETA per soglia via intersezione quadratica retta/circonferenza; stato "già raggiunto" sempre dall'ultima lettura osservata, mai dalla retta stimata; intervallo di confidenza via bootstrap (500 repliche, percentili 5°-95°, + quota di repliche che raggiungono la soglia)
  - **Aggiunta emersa in fase di test** (non nel design originale): un tetto all'orizzonte di estrapolazione (`MAX_EXTRAPOLATION_MULTIPLE`, 10× lo span osservato) — senza tetto, uno slope quasi-zero ma non nullo produceva ETA proiettate a secoli/millenni (visto live su Piano 2). Oltre il tetto → "non raggiunto sul trend attuale" invece di una data assurda
  - **Secondo bug emerso in fase di test**: Pianterreno mostrava anch'esso 0.0000 mm/week nonostante uno spostamento netto reale di 0.407mm. Causa: letture identiche ripetute su molte date consecutive (13 letture ma solo 3 valori distinti — plausibile con precisione di lettura di 0.25mm e movimento lento) generavano decine di coppie "slope zero" ridondanti (stesso valore statico contato C(k,2) volte) che affogavano il segnale reale nella mediana pesata di Theil-Sen. Fix: `dedupeConsecutiveReadings.js` (`src/utils/`) collassa le run di letture consecutive identiche (stesso x e y) prima del fit, tenendo la più recente della run. Applicato in `useETAPredictions`. **Piano 1 e Piano 2 restano correttamente a 0.0000 mm/week dopo il fix** — non è lo stesso bug: Piano 1 è genuinamente piatto (prima lettura = ultima lettura), Piano 2 oscilla molto (2.7mm di percorso totale contro 0.28mm di spostamento netto) senza una direzione consistente, quindi un trend robusto vicino a zero è l'esito onesto, non un difetto
    - **Verificato numericamente** (non solo per arrotondamento di stampa): per Piano 2, `bx == 0.0` è un valore Python letterale, non un numero minuscolo troncato in visualizzazione. Causa: 8 delle 45 coppie pesate hanno slope esattamente zero perché la lettura a 0.25mm di precisione produce lo stesso valore x su date diverse (es. t=269 e t=610 leggono entrambi esattamente 0.25); quelle coppie pesano solo il 17.8% del peso totale, ma l'oscillazione di Piano 2 è abbastanza simmetrica sopra/sotto quei valori ripetuti che il punto di mediana pesata (50% del peso cumulato) cade comunque esattamente su una di esse. Con `A = bx²+by² = 0` esatto, `ETASolver` restituisce "non raggiunto" dal primo ramo (nessun drift), prima ancora di valutare il discriminante
  - Limite noto, non risolto da questo design: un errore nella primissima lettura di un piano diventa un offset costante indistinguibile in tutte le letture normalizzate successive (l'origine della normalizzazione non è un dato privo di errore)
- [x] Attività vs. tendenza direzionale: un piano molto "attivo" (es. Piano 2, oscillante, 2.7mm di percorso totale contro 0.28mm di spostamento netto) mostrava "not reached" ovunque nel pannello ETA pur essendo segnalato come "Most Active" — non un bug, ma le due metriche rispondono a domande diverse (attività cumulativa vs. progresso netto direzionale) e la vecchia UI non lo chiariva. Soluzione adottata: **non** una seconda ETA sull'attività, ma un indicatore visivo relativo:
  - `ActivityHeatMeter.js` (`src/components/common/`) — barra di attività relativa (Weekly Rate Total Path, scalata sul massimo tra i piani correnti), **non** una proiezione; niente soglie assolute non giustificate da letteratura
  - Selettore di metodo ETA: registry pluggable `src/services/calculations/estimators/` (`TheilSenEstimator.js`, `SecantEstimator.js`, `index.js`) — stessa interfaccia (`compute`, `describe`, `methodology`, `label`), aggiungibile con altri metodi in futuro. `SecantEstimator` reimplementa fedelmente il vecchio metodo a due punti (verificato: riproduce esattamente i numeri pre-v0.16.0) come baseline di confronto, non come raccomandazione
  - Selettore "ETA Method" nella sezione Movement Summary; governa Trend Rate, pannello ETA per piano e Top 5 Soonest ETAs; l'Activity Heat Meter resta indipendente dal metodo selezionato
  - ETA per soglia mostra di nuovo "(X from first reading)" oltre al tempo rimanente da oggi (richiesto esplicitamente — coerenza con la UI precedente)
  - Test unitari: `TheilSen.test.js`, `ETASolver.test.js`, `dedupeConsecutiveReadings.test.js`, `SecantEstimator.test.js` (25 test totali)
- [x] Timeline view: aggiunto uno switch ("Space readings proportionally to time") per passare dalla spaziatura equidistante (asse categoriale, comportamento originale, resta il default) a una spaziatura proporzionale ai giorni reali (asse numerico su timestamp, con `domain={['dataMin','dataMax']}` e tick formattati come date) — `TimelineView.js`. **Bug trovato in fase di test**: con spaziatura proporzionale il tooltip mostrava il timestamp grezzo invece della data, perché `Tooltip`'s `labelFormatter` non viene applicato automaticamente quando si usa un `content` personalizzato — Recharts lo passa come prop e tocca al componente custom invocarlo. Fix in `CustomTooltip.js` (accetta e applica `labelFormatter` se presente)
- [x] "Overall Movement Direction" nella Movement Summary usava ancora la logica pre-v0.16.0 (solo ultima lettura vs. prima, letture intermedie ignorate) invece del metodo ETA selezionato — su Piano 2 mostrava "→ Expanding & ↓ Sinking" anche quando il pannello ETA sopra diceva "not reached" per ogni soglia (Theil-Sen), apparendo contraddittorio: erano due calcoli scollegati, non un'inconsistenza nei dati. Fix: `direction: {x, y}` aggiunto al ritorno di entrambi gli estimator (`TheilSenEstimator`: slope della retta stimata; `SecantEstimator`: vettore prima→ultima lettura, invariato) e propagato da `useETAPredictions`; l'etichetta e la frase ("Based on the fitted trend" / "Based on the net change from first to last reading") ora riflettono il metodo selezionato e cambiano dinamicamente con lo switch
  - **Bug trovato in fase di test**: la soglia `Math.abs(dir) > 0.01` ereditata dalla vecchia logica tagliava fuori la frase descrittiva per Theil-Sen, perché `direction` lì è un rate (mm/giorno, es. 0.0006) non uno spostamento assoluto in mm — soglia tarata sulla scala sbagliata, la frase risultava troncata ("...normalized data shows." senza seguito) anche quando l'intestazione sopra mostrava correttamente una direzione. Fix: check a zero esatto (`dirX !== 0`) invece di soglia in mm, coerente con la logica già usata per l'intestazione
  - Test: `TheilSenEstimator.test.js` (nuovo), `SecantEstimator.test.js` (aggiornato) — 28 test totali
- [x] Analisi separata delle componenti orizzontale/verticale per l'ETA (richiesta esplicita: capire se la causa di sprofondamento (verticale) sia più forte della spinta verso l'esterno del muro (orizzontale)):
  - Selettore "Displacement Component" (Combined 2D / Horizontal only / Vertical only) accanto a "ETA Method" nella Movement Summary; governa Trend Rate, pannello ETA per piano e Top 5 Soonest ETAs — entrambi gli assi erano già fittati indipendentemente da `RegressionAnalyzer`, quindi bastava un solver 1D dedicato invece di duplicare la macchina quadratica 2D: `LinearThresholdSolver.js` (`src/services/calculations/`), usato da entrambi gli estimator quando `component !== 'combined'` e da `BootstrapEstimator` (nuovo parametro opzionale `axis`)
  - Coppia di selettori estratta in `ETAControls` (componente locale in `CrackMovementVisualizer.js`, stesso stato condiviso) e ripetuta anche nell'header di "Structural Analysis Summary" e fra una card piano e la successiva, non solo in cima a Movement Summary — richiesto esplicitamente per non dover risalire la pagina per cambiare metodo/componente
  - Riga "Horizontal vs. Vertical Rate" **sempre visibile** (indipendente dal selettore di componente) con i due rate firmati mm/settimana e un'indicazione testuale di quale componente sia attualmente il driver più forte — per rispondere direttamente alla domanda originale senza dover cambiare selettore
  - Rappresentazione grafica aggiunta su richiesta esplicita, in due punti: `RateComparisonBar.js` (`src/components/common/`) — coppia di barre per piano, sostituisce il testo nella card del singolo piano; `RateComparisonSummary.js` (idem) — stesso componente ripetuto per ogni piano nella Structural Analysis Summary, per il confronto cross-piano che la vista per-piano non dà
    - **Iterazione su feedback esplicito**: prima versione delle barre era divergente (segno → sinistra/destra dal centro) e il confronto cross-piano usava un grafico a barre raggruppate Recharts. Feedback: le barre per-piano piacevano, il grafico Recharts no, e serviva confrontare l'intensità di orizzontale/verticale "in modo immediato". Fix: barre ora sempre nella stessa direzione (solo valore assoluto, crescono da zero), il segno resta solo nella parola accanto al numero ("expanding"/"closing", "rising"/"sinking") — così la lunghezza della barra è l'unico segnale da leggere per capire quale componente è più forte. `ComponentRateChart.js` (Recharts) rimosso, sostituito da `RateComparisonSummary.js` che riusa `RateComparisonBar` per ogni piano con una scala condivisa (`maxAbs`) fra tutti i piani, invece di una scala locale per piano
    - **Secondo giro di feedback**: la normalizzazione delle barre era comunque fuorviante — scala locale (per piano) per le card, scala globale per il riepilogo cross-piano, quindi lo stesso valore risultava lungo in modo diverso a seconda di dove veniva mostrato; inoltre il valore massimo corrente riempiva il 100% della barra, dando l'impressione di un tetto assoluto che non esiste. Fix: `maxComponentRate` calcolato una sola volta in `CrackMovementVisualizer.js` (max assoluto fra tutti i piani e i due assi, per il metodo ETA selezionato) e passato come `maxAbs` sia alle barre per-piano che a `RateComparisonSummary` — stessa scala ovunque; `MAX_FILL_PERCENT = 80` in `RateComparisonBar.js` lascia margine visivo oltre il valore massimo corrente
    - **Terzo giro di feedback**: "stessa apparenza ma semantica diversa" rispetto ad altre barre già in uso nella dashboard (es. Activity Heat Meter) — la barra generica comunicava "percentuale di completamento", fuorviante per un confronto di intensità fra due rate. Prototipate due alternative fuori dall'app (artifact di confronto: freccia vettoriale vs. scatter a quadranti, entrambe su dati reali + un piano sintetico dichiarato per lo stress-test del caso verticale-dominante) e scelta la freccia vettoriale. Sostituisce sia `RateComparisonBar.js` che `RateComparisonSummary.js` (entrambi rimossi): `VectorRateArrow.js` (`src/components/common/`) — un'unica freccia SVG per piano dall'origine, angolata secondo il rapporto H/V reale e colorata in base all'asse dominante (blu = orizzontale, ocra = verticale, grigio = nessun movimento netto → cerchietto vuoto invece della freccia); assi ed etichette di scala disegnati nell'SVG stesso. `VectorRateArrowGrid.js` ripete la stessa figura per piano nella Structural Analysis Summary. Scala condivisa invariata nel principio ma ora arrotondata a un valore "pulito" (`niceCeil.js`, `src/utils/`) invece del massimo esatto, cosicché le etichette degli assi mostrino un riferimento leggibile
      - **Bug trovato in fase di test**: nel riepilogo cross-piano la punta della freccia mancava per i piani col nome contenente uno spazio ("Piano 1", "Piano 2") quando il rate era non nullo — `VectorRateArrowGrid` passava il nome visualizzato come `id` del marker SVG, e `url(#rate-arrowhead-Piano 2)` con lo spazio non escapato è un riferimento non valido, quindi il marker falliva silenziosamente. Fix: `VectorRateArrow.js` ora sanifica sempre `id` internamente invece di fidarsi del chiamante. Corretto anche il secondo problema segnalato: le figure nel riepilogo non avevano un vincolo di larghezza e si allargavano fino a riempire la colonna della grid, molto più grandi delle card per piano — ora limitate a 140px come le altre
  - `ETA_COMPONENTS` / `DEFAULT_ETA_COMPONENT` (`regressionConfig.js`); `TheilSenEstimator`/`SecantEstimator.compute()` accettano ora `component` e ritornano anche `componentRates` (rate firmati per asse, indipendenti dal componente richiesto); `useETAPredictions` ricalcola tutti e tre i componenti per ogni piano/metodo (`estimates[metodo].components[componente]`), con "già raggiunto" specifico per componente (`|x|`, `|y|`, o `hypot(x,y)` a seconda del caso)
  - Verificato in browser: con letture puramente orizzontali (y costante), il crossing 2D combinato e quello orizzontale coincidono esattamente, come atteso matematicamente
  - Test: `LinearThresholdSolver.test.js` (nuovo, 5 test), `TheilSenEstimator.test.js` + `SecantEstimator.test.js` (aggiornati con test sul parametro `component` e su `componentRates`) — 37 test totali
- [x] Set di icone per lo stato di movimento (richiesta esplicita: rappresentare visivamente le 9 combinazioni orizzontale/verticale — 3 stati per asse, cardinalità 3² = 9 — nella Top 5 Soonest ETAs, al posto del pallino generico):
  - Icona a "prospetto crepa": due blocchi muro, il sinistro è il riferimento fisso (prima lettura), il destro è la posizione attuale — il gap fra i due codifica lo stato orizzontale (chiuso/nominale/largo), il loro sfasamento verticale codifica lo stato verticale; frecce di accento blu (orizzontale) / ocra (verticale) solo quando quell'asse non è "still"
  - `movementStates.js` (`src/constants/`) — deriva lo stato 3×3 da `dirX`/`dirY`, unica fonte di verità condivisa con il testo di "Overall Movement Direction" (stesse etichette)
  - Architettura pensata esplicitamente per essere sostituibile (richiesto: "plug and play" per un set futuro di tipo diverso, es. GIF): ogni set implementa un contratto `resolve(hState, vState, opts) => ReactNode`, registrato in `src/components/common/icons/movementIconSets/` (`svgIconSet.js` è l'unico finora) e selezionato in un unico punto (`icons/index.js`, `ACTIVE_MOVEMENT_ICON_SET`); `MovementIcon.js` è l'unico consumer, non importa mai un set specifico
  - Prototipata prima come artifact standalone (matrice 3×3 con intestazioni di riga/colonna = gli stati stessi, letture reali + dominio testato) prima di integrarla nell'app
  - Nella Top 5 Soonest ETAs l'icona è ridotta in base al Displacement Component selezionato: Horizontal/Vertical only forzano l'altro asse a "still" invece di mostrare uno stato che quella proiezione non usa affatto (un'ETA solo-orizzontale non guarda mai il trend verticale) — non serve un set di icone nuovo, sono semplicemente la riga/colonna centrale (asse forzato a "still") della stessa matrice 3×3
  - Considerata e scartata generazione di immagini via AI per l'icona: nessun tool di image generation disponibile in questa sessione; inoltre 9 icone devono restare un set internamente coerente (stessi scarti di gap/offset comparabili fra loro), cosa che l'SVG parametrico garantisce per costruzione e la generazione per immagine no
- [x] "Horizontal vs. Vertical Rate" spostato fuori dalla griglia di statistiche del piano e affiancato a "Overall Movement Direction" (richiesto esplicitamente) — i due spiegano lo stesso trend ed erano separati da campi non correlati
  - Anche "Weekly Rate (Total Path)" spostato nella stessa sezione sotto lo stesso `<hr>` (richiesto esplicitamente): le tre card ora condividono la stessa riga sotto il divisore, invece di essere sparse nella griglia principale delle statistiche
    - **Bug scoperto dal feedback**: la sezione "Overall Interpretation" era in realtà annidata come UN singolo elemento della griglia a 2 colonne delle statistiche del piano (non un fratello successivo, come sembrava dal solo commento "End of grid" — verificato analizzando l'indentazione/l'AST reale col parser Babel), quindi occupava solo metà larghezza della card. Weekly Rate, richiesto "colspan=2" per dare più spazio alla sua barra, andava effettivamente in overflow su una riga propria per mancanza di spazio. Fix: `md:col-span-2` sulla sezione "Overall Interpretation" stessa (non sui singoli elementi), così occupa l'intera larghezza della card; al suo interno passata da griglia fissa a `flex flex-wrap` con `min-w` per evitare che le etichette vadano a capo in modo scomodo, e Weekly Rate con `flex-[2]` per il doppio dello spazio relativo alle altre due
  - Scambiate di posizione "ETA to Displacement Thresholds" e "Weekly Rate (Total Path)" (richiesto esplicitamente): ETA to Displacement Thresholds ora nella riga larga di "Overall Interpretation" (dove aveva più spazio verticale per la lista delle soglie), Weekly Rate torna nella griglia principale accoppiato a Trend Rate
- [x] Peso di "densità di conferma" per le letture deduplicate (domanda esplicita: 60 letture giornaliere identiche venivano trattate come equivalenti a 2 letture distanziate 60 giorni, perdendo l'informazione che una conferma più densa riduce l'incertezza sulla genuinità della stasi):
  - `dedupeConsecutiveReadings.js` ora porta `runLength` (quante letture grezze sono state assorbite in quel punto) invece di scartare il conteggio
  - `computeConfirmationWeight(runLength)` (`regressionConfig.js`) — boost scalato come `sqrt(runLength)`, non lineare: un conteggio lineare riprodurrebbe esattamente il bug C(k,2) che il dedup esiste per evitare; lo scaling a radice quadrata rispecchia come l'errore standard di una media si riduce con la radice del numero di osservazioni indipendenti
  - Sicurezza per costruzione, non per un tetto aggiunto: il peso di coppia in `TheilSen.fit` è `min(peso_i, peso_j)`, quindi un punto con boost molto alto non può da solo dominare una coppia con un punto meno confermato — il boost si somma solo quando ENTRAMBI i punti della coppia sono ben confermati. Verificato con un test dedicato: un punto fuori linea confermato 100 volte non sposta la mediana pesata se i suoi vicini hanno peso 1
  - Verificato sui dati reali: su Pianterreno (unico piano con un pattern di run non banale, `runLengths: [2, 9, 2]`) lo slope stimato non cambia — il punto col peso più basso resta lo stesso anche dopo il boost, quindi la mediana pesata cade sulla stessa coppia di prima; l'effetto si propaga comunque nell'ampiezza dell'intervallo di confidenza bootstrap, non nel punto stimato
  - Test: `dedupeConsecutiveReadings.test.js` (aggiornato, +1 test su un run lungo), `RegressionAnalyzer.test.js` (nuovo, 3 test) — 41 test totali
- [x] Rimosso il selettore "Displacement Component" e tutto il calcolo ETA per singolo asse che lo sosteneva (richiesta esplicita: l'informazione utile della scomposizione orizzontale/verticale è già data dalla figura "Horizontal vs. Vertical Rate", sempre visibile e indipendente dal selettore — il pannello ETA a singolo asse era ridondante, non un'informazione aggiuntiva):
  - `LinearThresholdSolver.js` + test eliminati; `BootstrapEstimator` torna a fare sempre il fit combinato 2D (parametro `axis` rimosso); `TheilSenEstimator`/`SecantEstimator.compute()` non accettano più `component`, risolvono sempre la soglia 2D — `componentRates` resta invariato (usato dalla figura vettoriale, non dal selettore)
  - `useETAPredictions` torna a una struttura piatta `estimates[metodo] = { rateMmPerWeek, thresholds, direction, componentRates }`, non più annidata per componente; "già raggiunto" torna a usare sempre `hypot(x, y)`
  - `ETA_COMPONENTS` / `DEFAULT_ETA_COMPONENT` rimossi da `regressionConfig.js`; `ETAControls` torna a un solo selettore ("ETA Method"); `MovementIcon` non accetta più `component` — mostra sempre lo stato reale su entrambi gli assi, non più ridotto in base al selettore (dato che il selettore stesso non esiste più)
  - Test: rimossi i test su `component`/`LinearThresholdSolver`, aggiornati quelli su `componentRates` per non dipendere più dal parametro — 34 test totali
- [x] Titolo "Activity Gauge" sopra la barra di attività relativa (richiesto esplicitamente), esteso subito dopo a "Activity Gauge: <assessment>" con la parola di valutazione (Lower/Moderate/Highest/No measurable) color-coded col colore del piano — logica di soglia estratta in `getActivityAssessment` (`ActivityHeatMeter.js`) così l'etichetta nel titolo e la barra condividono la stessa fonte di verità; la parola non è più ripetuta sotto la barra (restava solo la frase esplicativa)
- [x] Vista "Normalized Movement" rimossa dal selettore "View" (richiesto esplicitamente: l'informazione utile è già in "Movement Patterns" tramite il bottone raw/normalized già presente, che passava semplicemente da una vista top-level all'altra) — `MovementPatternsView.js` e `NormalizedMovementView.js` erano lo stesso componente duplicato due volte (stessa struttura, differivano solo per `useNormalized`, titolo e testo descrittivo); unificati in un solo `MovementPatternsView.js` con stato locale `useNormalized` (default `true`, come richiesto) che il bottone alterna internamente invece di saltare fra due voci del selettore "View". `NormalizedMovementView.js` eliminato
  - Nella stessa richiesta: "Space readings proportionally to time" nella Timeline ora è **on di default** (`proportionalSpacing` inizializzato a `true` invece di `false`) — resta comunque disattivabile dallo switch esistente
- [x] Figura "Horizontal vs. Vertical Rate" per piano allargata da 180px a 270px (richiesto esplicitamente, da uno screenshot mobile: la figura risultava piccola e circondata da spazio vuoto nella sua colonna) — cresce fino a riempire la colonna disponibile invece di restare fissa a una larghezza troppo stretta per il layout stretto da mobile
  - Stesso allargamento (140px → 270px) applicato anche alle figure per piano nella Structural Analysis Summary (`VectorRateArrowGrid.js`), richiesto esplicitamente per coerenza di dimensione con le card per piano
    - **Bug scoperto dal feedback (verificato solo su mobile reale, non riproducibile con `resize_window` in questa sessione)**: l'allargamento a 270px non aveva alcun effetto visibile su mobile. Causa: sotto il breakpoint `sm:` (640px) la griglia usava `grid-cols-2`, che forza due colonne a prescindere dallo spazio disponibile — ogni figura restava tagliata a metà schermo ben prima che il suo `max-width` potesse contare. Le card per piano invece usano `flex flex-wrap`, che permette di andare a capo su una riga propria quando lo spazio manca. Fix: griglia cambiata a `grid-cols-1` (una colonna, piena larghezza, sotto i 640px) `sm:grid-cols-3`
  - Aggiunta su richiesta esplicita: ogni figura nella Structural Analysis Summary è ora cliccabile/toccabile indipendentemente, per passare dalla dimensione piena al 50% e viceversa (utile per scorrere rapidamente i tre piani una volta capito quale interessa in dettaglio) — stato locale per piano in `VectorRateArrowGrid.js`, funziona sia con click che con tap
    - **Raffinamento su richiesta esplicita**: il click doveva essere un singolo evento condiviso, non indipendente per piano — cliccare una qualsiasi delle tre figure ora le ridimensiona tutte insieme (sono pensate per essere lette come un gruppo, non confrontate una alla volta). Estratto `ResizableFigure.js` (`src/components/common/`) — non controllato di default (stato interno proprio, usato dalla figura singola per piano), oppure controllato tramite `shrunk`/`onToggle` (usato da `VectorRateArrowGrid` per condividere un solo stato fra le tre)
    - **Layout su richiesta esplicita**: griglia della Structural Analysis Summary cambiata da 3 a 2 colonne (`sm:grid-cols-2`) — con 3 elementi, la terza va a capo da sola, allineata a sinistra sotto la prima, invece di essere distribuita fra 3 colonne pari
    - **Estensione su richiesta esplicita**: anche la figura per piano (nelle card individuali di Movement Summary) è ora cliccabile/toccabile per passare al 50% e viceversa, riusando `ResizableFigure.js` in modalità non controllata (ridimensiona solo se stessa)
    - **Unificazione su richiesta esplicita**: tutte le figure "Horizontal vs. Vertical Rate" della pagina (per piano E Structural Analysis Summary) condividono ora un solo stato/trigger — cliccarne una qualsiasi le ridimensiona tutte insieme. Stato sollevato in `CrackMovementVisualizer.js`, passato come `shrunk`/`onToggle` sia alla figura per piano sia a `VectorRateArrowGrid` (che non gestisce più uno stato proprio)
- [x] Blocchi di testo esplicativo sempre visibili sostituiti da un disclosure collassato di default (richiesta esplicita, dopo osservazione che il box "Timeline shows normalized movement data..." intasava l'usabilità principale della pagina; poi estesa ad altri punti simili su richiesta di verificarne la presenza altrove):
  - `InfoDisclosure.js` (`src/components/common/`) — normalmente un'etichetta compatta con icona (ⓘ) e simbolo che segnala sia "altra informazione disponibile" sia "cliccabile"; al click sostituisce se stessa con il box completo (non lo aggiunge sotto, resta un solo elemento sulla pagina), e il box espanso è a sua volta cliccabile per richiudersi. Usa `div role="button"` invece di `<button>` reale perché alcuni contenuti (es. `InterpretationNotes`) includono link, che non possono annidarsi validamente dentro un bottone
  - Applicato a: box "Timeline shows normalized movement data" in `TimelineView.js` (richiesta originale — etichetta "Timeline normalized view"); blocco descrittivo a punti in `MovementPatternsView.js` (stesso pattern, etichetta dinamica in base a raw/normalized); `InterpretationNotes.jsx`, mostrato in fondo a ogni pagina (etichetta "Measurement & interpretation notes")
  - **Verificato e lasciato invariato di proposito**: la legenda colori Raw/Normalized in `DataTableView.js` — è una legenda breve legata direttamente alla lettura della tabella subito sotto, non un paragrafo di prosa isolato, quindi non è lo stesso tipo di "clutter"
- [x] Icona di scope sul box "Cross Angle Analysis" in `SingleReadingView.js` (richiesta esplicita: il colore d'allerta (arancione) in questo dominio si legge come "problema strutturale reale", ma la deviazione angolare riguarda il processo di lettura/scatto — non la condizione reale del muro. Discusso e scartato un tentativo di distinguere algoritmicamente causa "camera" vs. "trascrizione": da una singola lettura le due sono matematicamente indistinguibili (stesso identico segnale geometrico), quindi un'icona che tentasse di indovinare la causa sarebbe fuorviante quanto il problema che deve risolvere):
  - Icona fotocamera + "!" in basso a destra nel box, dello stesso colore (arancione) — segnala solo lo *scope* dell'alert ("è un problema di lettura/scatto, non necessariamente movimento strutturale"), non una diagnosi di causa. Coerente col fatto che gli scatti sono fatti a mano libera finora
  - Non serve logica per-piano: `SingleReadingView.js` è generico rispetto al piano selezionato, quindi l'icona compare automaticamente per Pianterreno, Piano 1 e Piano 2 senza modifiche aggiuntive
  - **Reso collassabile su richiesta esplicita**: il box ora usa lo stesso pattern di `InfoDisclosure` (ⓘ, etichetta, freccia, collassa/espande in-place) invece di essere un box arancione statico — `InfoDisclosure.js` esteso con una prop `tone` ('info' blu di default, 'warning' arancione) così il colore di allerta resta ma il comportamento è coerente col resto dell'app. L'icona fotocamera + "!" si è spostata dentro l'etichetta del trigger collassato (non più in basso a destra nel box espanso), cosicché anche da collassato si veda subito lo scope dell'alert
  - **Rimosso "— not necessarily real structural movement" su richiesta esplicita**: la frase non aggiungeva informazione rispetto allo scope già dichiarato
  - **Ulteriore rifinitura su richiesta esplicita**: rimosso il "!" dall'etichetta del trigger collassato (ridondante con il fatto stesso che si tratti di un alert) e rimosso "(photos are taken freehand)" dalla riga di Scope nel box espanso (dettaglio non necessario al messaggio di scope); l'icona fotocamera è stata mantenuta anche nel testo espanso (non solo nel trigger collassato), così lo scope resta visibile sia chiuso che aperto
- [x] **Bug reale trovato durante la verifica** (domanda esplicita: "il trend rate non dovrebbe mai superare il rate di attività totale?"): per Pianterreno, Trend Rate (0.0044 mm/sett) risultava MAGGIORE di Weekly Rate Total Path (0.0037 mm/sett) — matematicamente non dovrebbe mai accadere (un rate netto direzionale non può superare il rate implicito nel percorso cumulativo). Causa: le prime due letture grezze di Pianterreno sono identiche, quindi `dedupeConsecutiveReadings` collassava questo "leading run" verso il suo membro più recente come da design — ma così facendo sostituiva silenziosamente t=0 (l'origine temporale della regressione) con il timestamp più tardo, accorciando la finestra osservata da 797 a 642 giorni e gonfiando lo slope. Un run finale non ha lo stesso problema (tenere il membro più recente preserva già l'ultima lettura vera); solo un run che parte dall'indice 0 lo ha. Fix: `dedupeConsecutiveReadings.js` ripristina sempre il `t` della vera prima lettura se `result[0]` viene sostituito. Trend Rate ora mostra 0.0036, correttamente sotto 0.0037. Piano 1/2 non sono affetti (nessuno dei due ha un run duplicato sulla propria prima lettura)
  - Test: `dedupeConsecutiveReadings.test.js` (2 nuovi test: leading run preserva t=0, trailing run invariato; 1 test esistente aggiornato) — 36 test totali
- [x] **Correzione su richiesta esplicita, da screenshot**: la griglia `sm:grid-cols-2` scelta al punto precedente per la Structural Analysis Summary tornava a rompersi a piena larghezza desktop non appena tutte e tre le figure erano a dimensione piena — Piano 2 andava a capo da solo su una riga propria invece di stare affiancato agli altri due. Il numero di colonne ora dipende sia dal viewport sia dallo stato shrunk: `sm:grid-cols-3` (desktop, sempre tutte e tre sulla stessa riga, a qualunque dimensione) più `grid-cols-1`/`grid-cols-2` sotto i 640px (una colonna a dimensione piena, due colonne — 2 righe, allineate a sinistra — una volta ridotte)
  - Anche in questo caso il trigger di ridimensionamento resta condiviso fra tutte le figure "Horizontal vs. Vertical Rate" della pagina (per piano e Structural Analysis Summary), invariato dall'unificazione precedente
- [ ] Revisione del linguaggio: coerenza generale e glossario
- [ ] CI/CD automatically replace v.number in README and add entry in CHANGELOG and TODO

##### v0.15.0 
- [x] Estrarre sezioni Footer e Interpretation Notes dal main component in componenti dedicate
- [x] CI/CD script di aggiornamento automatico della versione ("npm version [patch|minor|major]")


##### v0.14.0 
- [x] Refactoring SOLID del codice

##### v0.13.2 
- [x] Logo (icona webapp e logo)

##### v0.13.1 
- [x] Uso più consistente delle etichette (absolute → raw; raw data → data o dataset)

##### v0.13.0 
- [x] Refactoring del codice (ridondanza funzioni, variabili di configurazione globali, ...)

##### v0.12.0 
- [x] Fattorizzare il calcolo delle coordinate dalle singole view
- [x] Aggiungere tooltip info on single reading view
- [x] Aggiungere tooltip info on raw ("movement patterns") and normalised ("normalized movements") datapoints

##### v0.11.1 
- [x] FINIRE PROOF-READING della seconda metà del file del METODO

##### v0.11.0 
- [x] Review documentation and footer (readme, method, license)

##### v0.9.0 
- [x] Sommario complessivo: 1) Crepa più attiva 2) Crepa più ampia 3) Ordine di arrivo delle prossime 5 soglie (secondo media aritmetica)
- [x] CI/CD: consistenza del set delle immagini dei rilievi
- [x] CI/CD: standard checks (lint, dependencies, ...)
- [x] CI/CD: auto-deploy on main branch pass

##### v0.8.0 
- [x] Aggiunte previsione di arrivo per soglie di distaccamento notevoli (1mm, 2mm, e 5mm) per piano
- [x] SINGLE READING VIEW: distinguere il calcolo geometrico tra coordinate centrali e rappresentazione della croce nel riquadro ridotto (20x10 → 3x3)
- [x] Aggiunta dei marker di proiezione dei punti up, right, down, left sui bordi del riquadro visibile (3x3)
- [x] Sezione dataset: aggiunta controllo per il download in vari formati
- [x] Sezione dataset: non solo raw reading ma anche normalized data e ortogonalità della croce

##### v0.7.0 
- [x] Single reading view: controllo per il download dell'immagine del rilievo
- [x] Sezione dataset: controllo per il download del set di immagini completo

##### v0.6.0 
- [x] Miglioramento lettura sinottica tra raw reading e normalized reading
- [x] Proofreading delle legende e uso consistente del color-coding project-wide


### Backlog

#### Alta Priorità

- [ ] Aggiungere autenticazione utente per progetti di monitoraggio privati
- [ ] Implementare persistenza dei dati (salvare i rilievi su database)
- [ ] Migliorare la responsività mobile per l'uso sul campo
- [ ] Creare funzionalità di generazione report PDF
- [ ] Aggiungere notifiche email per avvisi di soglia
- [ ] Unit testing infrastructure + core tests
- [ ] Integration tests + error handling
- [ ] Performance optimization + accessibility
- [ ] Validare gli input dei reading (evitare propagazione silenziosa di `NaN` da valori malformati in `IntersectionCalculator`/`AngleAnalyzer`, che può corrompere silenziosamente la normalizzazione di un intero piano se colpisce la prima lettura)

#### Media Priorità

- [ ] Supporto multilingua (toggle Italiano/Inglese)
- [ ] Tema dark mode
- [ ] Aggiungere toggle unità di misura (mm/pollici)
- [ ] Implementare importazione dati da CSV/Excel
- [ ] Aggiungere vista di confronto tra più periodi temporali
- [ ] Valutare l'aggiunta di un datapoint sintetico al 2017-01-01 per Pianterreno (P0), con lo stesso valore della sua prima lettura reale, per rappresentare nel fit il ricordo di famiglia che la crepa esisteva già allora (memoria: `p0-crack-predates-2017`). Semanticamente è comunque una lettura, per quanto approssimata. **Punto aperto, non risolto**: verificato numericamente che un simile punto, dato pieno peso in Theil-Sen, riduce lo slope stimato a ~18% del valore attuale (ETA proiettate ~5.5× più lontane) — le coppie che lo coinvolgono hanno Δt enorme (~7 anni) e trascinano la mediana pesata verso il basso più di quanto l'informazione reale giustifichi. Da riconsiderare con un meccanismo che rifletta la bassa confidenza del punto (peso esplicito ridotto, o un framework bayesiano con incertezza) invece di un punto a peso pieno, prima di implementare

#### Bassa Priorità

- [ ] Aggiungere scorciatoie da tastiera per la navigazione
- [ ] Implementare undo/redo per l'inserimento dati
- [ ] Aggiungere vista ottimizzata per la stampa
- [ ] Creare tutorial/flusso di onboarding per nuovi utenti
- [ ] Miglioramenti per l'accessibilità (etichette ARIA, supporto screen reader)
- [ ] Perfezionare i bordi della griglia nelle varie view (poco visibili a destra e in basso)

#### Debito Tecnico

- [ ] Ottimizzare le performance di rendering SVG per dataset grandi
- [ ] Aggiungere test unitari completi (coperti finora solo `TheilSen`/`ETASolver`; `RegressionAnalyzer`, `BootstrapEstimator`, `useETAPredictions` e tutto il resto dell'app restano senza test)
- [x] Sostituire il placeholder di test in `App.test.js` (verificava testo "learn react" ereditato dal boilerplate CRA, mai esistito in questa app) con un'asserzione reale sull'heading del dashboard — richiedeva anche installare `@testing-library/jest-dom`/`@testing-library/react` (mancanti da `package.json` nonostante `setupTests.js` li richiedesse: `npm test` falliva ancora prima di eseguire un solo test) e un mock di `ResizeObserver` in `setupTests.js` (richiesto da Recharts, assente in jsdom)
- [ ] Migliorare gestione errori e feedback utente
- [ ] Aggiungere stati di caricamento per operazioni asincrone
- [ ] Calcolare le metriche di distanza/rate *descrittive* (Direct Displacement, Total Path Distance in `CrackMovementVisualizer.js`) sui dati normalizzati invece che raw — oggi coincidono numericamente perché shift e flip sono isometrie, ma restano un'equivalenza implicita e fragile. (Il nuovo Trend Rate/ETA regressivo usa già i dati normalizzati.)
- [x] Rimosso il codice morto dell'accumulatore `grandTotalDistance` commentato in `CrackMovementVisualizer.js`
- [ ] Fattorizzare il parsing dei reading (`[up,right,down,left]` + confini fisici) duplicato in `IntersectionCalculator`, `AngleAnalyzer` e `SingleReadingView`

#### Documentazione

- [ ] Creare video tutorial per l'utilizzo
- [ ] Aggiungere documentazione API se viene aggiunto un backend
- [ ] Creare linee guida per contribuire (CONTRIBUTING.md)
- [ ] Aggiungere guida alla risoluzione dei problemi
- [ ] Documentare il processo di deployment
- [ ] METODO.md: chiarire che `toSVGX`/`toSVGY` nell'esempio di conversione mm→pixel operano in realtà sul `DISPLAY_RANGE` zoomato (±1.5mm), non sui confini fisici del crack-meter (±20/±10mm) — la Single Reading View estrapola le linee della croce fino al bordo del riquadro ±1.5mm, cosa non descritta nel METODO
- [x] METODO.md: documentare il nuovo metodo di calcolo dell'ETA (regressione Theil-Sen pesata per ortogonalità + intervallo di confidenza bootstrap, v0.16.0)

#### 1.0.0 milestone's should-haves:

- Comprehensive unit tests (>80% coverage)
- Integration tests for key workflows
- Error boundaries and graceful degradation
- Production-ready build optimization
- Documentation (API docs, architecture diagrams)
- Accessibility compliance
- Performance benchmarks met
- Security audit passed
- Browser compatibility tested
